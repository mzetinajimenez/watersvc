# MongoDB connection string
# Replace with your actual MongoDB Atlas connection string or local MongoDB URI
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/watersvc?retryWrites=true&w=majority

# Default timezone for users (IANA timezone format)
DEFAULT_TIMEZONE=UTC

# Default daily water goal in ounces
DEFAULT_DAILY_GOAL_OZ=64.0
